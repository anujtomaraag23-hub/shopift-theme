{% if preheading or heading or subheading %}
    <div class="heading heading-wrap {{ class }} {{ heading_alignment_mobile }} {{ heading_alignment }}">
        <div class="row d-flex align-items-end justify-content-between">
            <div class="col-12 col-md-6 col-lg-7">
                {% if preheading != blank %}
                    <h6 class="sub-1 preheading fs-25 mb-2 text-uppercase opacity-85"{% if animation != "off" %} data-aos="fade-up"{% endif %}>
                        {{ preheading }}
                    </h6>
                {% endif %}
                {% if heading %}
                <{{ heading_tag }} class="sub-2 heading mb-0 {{ heading_size }}
                "{% if animation != "off" %} data-aos="fade-up" data-aos-delay="100"{% endif %}>
                {{ heading }}
            </{{ heading_tag }}>
            {% endif %}
            {% if subheading != blank %}
                <div class="sub-3 subheading rte mb-0 mt-2 {{ subheading_class }}"{% if animation != "off" %} data-aos="fade-up" data-aos-delay="200"{% endif %}>
                    {{ subheading }}
                </div>
            {% endif %}
        </div>
        <div class="col-12 col-md-6 col-lg-5 text-start text-lg-end">

            {% if button_link and button_label != null %}
                <div class="sub-4 card-bottom mt-3"{% if animation != "off" %} data-aos="fade-up" data-aos-delay="300"{% endif %}>
                    <a
                            {% if button_link == blank %}
                                role="link" aria-disabled="true"
                            {% else %}
                                href="{{ button_link }}"
                            {% endif %}
                            class="btn btn-{{ button_style }} position-relative z-index-1"
                    >
                        <span>{{- button_label | escape -}}</span>
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
    </div>
{% endif %}


